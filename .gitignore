# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment files (contain secrets!)
.env
.env.local
.env.production
.env.*.local

# Vagrant
.vagrant

# CKAN data — NEVER commit data
migration/
vm-backup/
data/
*.dump
*.tar.gz
*.sql.gz

# Backup files
*.bak
*.backup
*~

# Logs
*.log
logs/

# CKAN runtime artifacts
ckan_storage/
solr_data/
pg_data/
pip_cache/

# CKAN dev placeholders
local/*
_service-provider/*
_solr/schema.xml
_src/*

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.eggs/

# Node (if any frontend)
node_modules/
npm-debug.log*

# Temporary files
tmp/
temp/
favicon_io/

# ----------------------------------------------------------------------
# Confidential / personal artifacts — never commit, distribution is RESTRICTED.
# Source files (raw banners, WhatsApp drops, raw pentest PDF) are kept locally
# only. Production-ready copies live under the theme's public/ directory.
# ----------------------------------------------------------------------
# Pentest deliverables (TLP:AMBER+STRICT)
Laporan_Pentest_*.pdf
reports/
docs/
tasks/

# Programmer work paper (internal admin)
/KERTAS*.pdf

# Raw source assets dropped at repo root (deployed copies live inside theme)
/BannerSatuData*.png
/BannerSatuData*.jpg
/BannerSatuData*.jpeg
/WhatsApp Image*
